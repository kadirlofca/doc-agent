# API keys are entered at runtime via the provider connect UI.
# Do NOT put API keys in frontend env files — they would be exposed to the browser.

# Backend URL (only used server-side in next.config.ts rewrites)
# NEXT_PUBLIC_API_URL=http://localhost:8000
